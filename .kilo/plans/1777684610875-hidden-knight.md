# Overhaul Quiz Design Plan

## Objective
Overhaul the UI of the existing React quiz application to match the provided single-file HTML/CSS design while preserving all existing quiz logic and content rendering capabilities (images, code snippets, tables).

## Phase 1: Styles & Assets (Global Setup)
- [ ] Move CSS from the new design into `src/index.css`. Ensure it handles the required theme (`#ff3500`, `#46f2e7`, `white`, `black`, `dark`).
- [ ] Update `index.html` to include the required Google Fonts (`Outfit`, `JetBrains Mono`, `IBM Plex Sans`) and FontAwesome library from the new design.

## Phase 2: Component Overhaul
Replace existing React JSX structures with the new HTML markup and CSS classes.

### 2.1 MainMenu.tsx
- [ ] Implement the `landing` screen structure.
- [ ] Replace custom cards/buttons with the new `.mode-card` class structure.

### 2.2 ActiveQuiz.tsx
- [ ] Implement the `quiz` screen structure (topbar, question area, nav buttons).
- [ ] Ensure existing logic for:
    - [ ] Code snippet rendering (`SyntaxHighlighter`)
    - [ ] Table rendering
    - [ ] Mermaid/Architecture diagram rendering
- [ ] Adapt CSS classes from the new design to replace Tailwind classes.

### 2.3 Results.tsx
- [ ] Implement the `results` screen structure.

### 2.4 ReviewGrid.tsx
- [ ] Implement the `review-overlay` structure as a component/modal.

## Phase 3: Integration & Logic
- [ ] Update `QuizContext` if necessary (unlikely to need major changes).
- [ ] Ensure `App.tsx` correctly switches between the new components based on `appState`.
- [ ] Integrate the boot animation logic into `main.tsx` or `App.tsx`.

## Phase 4: Verification
- [ ] Check rendering of tables, code snippets, and images in the new layout.
- [ ] Validate responsive behavior.
- [ ] Ensure no "white screen" issues due to incorrect asset paths.

---
## Questions for the User
1.  Are there any specific UI interactions from the original app (besides quiz navigation) that *must* be preserved?
2.  Do you want me to keep the `Lucide` icons or fully switch to the `FontAwesome` icons as shown in your HTML example? (I recommend FontAwesome to match your design).
